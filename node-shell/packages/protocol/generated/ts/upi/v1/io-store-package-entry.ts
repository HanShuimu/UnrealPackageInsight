// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class IoStorePackageEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IoStorePackageEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIoStorePackageEntry(bb:flatbuffers.ByteBuffer, obj?:IoStorePackageEntry):IoStorePackageEntry {
  return (obj || new IoStorePackageEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIoStorePackageEntry(bb:flatbuffers.ByteBuffer, obj?:IoStorePackageEntry):IoStorePackageEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IoStorePackageEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

packagePath():string|null
packagePath(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
packagePath(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

packageId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

firstChunkIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

chunkCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

firstPartitionIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

firstOffset():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

size():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

compressedSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

diskSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

order():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

hasPath():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startIoStorePackageEntry(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addPackagePath(builder:flatbuffers.Builder, packagePathOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, packagePathOffset, 0);
}

static addPackageId(builder:flatbuffers.Builder, packageId:bigint) {
  builder.addFieldInt64(1, packageId, BigInt('0'));
}

static addFirstChunkIndex(builder:flatbuffers.Builder, firstChunkIndex:number) {
  builder.addFieldInt32(2, firstChunkIndex, 0);
}

static addChunkCount(builder:flatbuffers.Builder, chunkCount:number) {
  builder.addFieldInt32(3, chunkCount, 0);
}

static addFirstPartitionIndex(builder:flatbuffers.Builder, firstPartitionIndex:number) {
  builder.addFieldInt32(4, firstPartitionIndex, 0);
}

static addFirstOffset(builder:flatbuffers.Builder, firstOffset:bigint) {
  builder.addFieldInt64(5, firstOffset, BigInt('0'));
}

static addSize(builder:flatbuffers.Builder, size:bigint) {
  builder.addFieldInt64(6, size, BigInt('0'));
}

static addCompressedSize(builder:flatbuffers.Builder, compressedSize:bigint) {
  builder.addFieldInt64(7, compressedSize, BigInt('0'));
}

static addDiskSize(builder:flatbuffers.Builder, diskSize:bigint) {
  builder.addFieldInt64(8, diskSize, BigInt('0'));
}

static addOrder(builder:flatbuffers.Builder, order:number) {
  builder.addFieldInt32(9, order, 0);
}

static addHasPath(builder:flatbuffers.Builder, hasPath:boolean) {
  builder.addFieldInt8(10, +hasPath, +false);
}

static endIoStorePackageEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIoStorePackageEntry(builder:flatbuffers.Builder, packagePathOffset:flatbuffers.Offset, packageId:bigint, firstChunkIndex:number, chunkCount:number, firstPartitionIndex:number, firstOffset:bigint, size:bigint, compressedSize:bigint, diskSize:bigint, order:number, hasPath:boolean):flatbuffers.Offset {
  IoStorePackageEntry.startIoStorePackageEntry(builder);
  IoStorePackageEntry.addPackagePath(builder, packagePathOffset);
  IoStorePackageEntry.addPackageId(builder, packageId);
  IoStorePackageEntry.addFirstChunkIndex(builder, firstChunkIndex);
  IoStorePackageEntry.addChunkCount(builder, chunkCount);
  IoStorePackageEntry.addFirstPartitionIndex(builder, firstPartitionIndex);
  IoStorePackageEntry.addFirstOffset(builder, firstOffset);
  IoStorePackageEntry.addSize(builder, size);
  IoStorePackageEntry.addCompressedSize(builder, compressedSize);
  IoStorePackageEntry.addDiskSize(builder, diskSize);
  IoStorePackageEntry.addOrder(builder, order);
  IoStorePackageEntry.addHasPath(builder, hasPath);
  return IoStorePackageEntry.endIoStorePackageEntry(builder);
}
}
