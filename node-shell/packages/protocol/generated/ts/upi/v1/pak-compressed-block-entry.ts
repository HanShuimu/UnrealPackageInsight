// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class PakCompressedBlockEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PakCompressedBlockEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPakCompressedBlockEntry(bb:flatbuffers.ByteBuffer, obj?:PakCompressedBlockEntry):PakCompressedBlockEntry {
  return (obj || new PakCompressedBlockEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPakCompressedBlockEntry(bb:flatbuffers.ByteBuffer, obj?:PakCompressedBlockEntry):PakCompressedBlockEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PakCompressedBlockEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

packageIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

blockIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

compressedStart():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

compressedEnd():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

compressedSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

diskSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

physicalStart():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

physicalEnd():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startPakCompressedBlockEntry(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addPackageIndex(builder:flatbuffers.Builder, packageIndex:number) {
  builder.addFieldInt32(0, packageIndex, 0);
}

static addBlockIndex(builder:flatbuffers.Builder, blockIndex:number) {
  builder.addFieldInt32(1, blockIndex, 0);
}

static addCompressedStart(builder:flatbuffers.Builder, compressedStart:bigint) {
  builder.addFieldInt64(2, compressedStart, BigInt('0'));
}

static addCompressedEnd(builder:flatbuffers.Builder, compressedEnd:bigint) {
  builder.addFieldInt64(3, compressedEnd, BigInt('0'));
}

static addCompressedSize(builder:flatbuffers.Builder, compressedSize:bigint) {
  builder.addFieldInt64(4, compressedSize, BigInt('0'));
}

static addDiskSize(builder:flatbuffers.Builder, diskSize:bigint) {
  builder.addFieldInt64(5, diskSize, BigInt('0'));
}

static addPhysicalStart(builder:flatbuffers.Builder, physicalStart:bigint) {
  builder.addFieldInt64(6, physicalStart, BigInt('0'));
}

static addPhysicalEnd(builder:flatbuffers.Builder, physicalEnd:bigint) {
  builder.addFieldInt64(7, physicalEnd, BigInt('0'));
}

static endPakCompressedBlockEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPakCompressedBlockEntry(builder:flatbuffers.Builder, packageIndex:number, blockIndex:number, compressedStart:bigint, compressedEnd:bigint, compressedSize:bigint, diskSize:bigint, physicalStart:bigint, physicalEnd:bigint):flatbuffers.Offset {
  PakCompressedBlockEntry.startPakCompressedBlockEntry(builder);
  PakCompressedBlockEntry.addPackageIndex(builder, packageIndex);
  PakCompressedBlockEntry.addBlockIndex(builder, blockIndex);
  PakCompressedBlockEntry.addCompressedStart(builder, compressedStart);
  PakCompressedBlockEntry.addCompressedEnd(builder, compressedEnd);
  PakCompressedBlockEntry.addCompressedSize(builder, compressedSize);
  PakCompressedBlockEntry.addDiskSize(builder, diskSize);
  PakCompressedBlockEntry.addPhysicalStart(builder, physicalStart);
  PakCompressedBlockEntry.addPhysicalEnd(builder, physicalEnd);
  return PakCompressedBlockEntry.endPakCompressedBlockEntry(builder);
}
}
