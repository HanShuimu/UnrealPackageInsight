// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class IoStoreCompressedBlockEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IoStoreCompressedBlockEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIoStoreCompressedBlockEntry(bb:flatbuffers.ByteBuffer, obj?:IoStoreCompressedBlockEntry):IoStoreCompressedBlockEntry {
  return (obj || new IoStoreCompressedBlockEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIoStoreCompressedBlockEntry(bb:flatbuffers.ByteBuffer, obj?:IoStoreCompressedBlockEntry):IoStoreCompressedBlockEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IoStoreCompressedBlockEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

blockIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

ownerTocEntryIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

partitionIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

offset():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

ucasOffset():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

compressedSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

diskSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

uncompressedSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

compression():string|null
compression(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
compression(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startIoStoreCompressedBlockEntry(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addBlockIndex(builder:flatbuffers.Builder, blockIndex:number) {
  builder.addFieldInt32(0, blockIndex, 0);
}

static addOwnerTocEntryIndex(builder:flatbuffers.Builder, ownerTocEntryIndex:number) {
  builder.addFieldInt32(1, ownerTocEntryIndex, 0);
}

static addPartitionIndex(builder:flatbuffers.Builder, partitionIndex:number) {
  builder.addFieldInt32(2, partitionIndex, 0);
}

static addOffset(builder:flatbuffers.Builder, offset:bigint) {
  builder.addFieldInt64(3, offset, BigInt('0'));
}

static addUcasOffset(builder:flatbuffers.Builder, ucasOffset:bigint) {
  builder.addFieldInt64(4, ucasOffset, BigInt('0'));
}

static addCompressedSize(builder:flatbuffers.Builder, compressedSize:number) {
  builder.addFieldInt32(5, compressedSize, 0);
}

static addDiskSize(builder:flatbuffers.Builder, diskSize:number) {
  builder.addFieldInt32(6, diskSize, 0);
}

static addUncompressedSize(builder:flatbuffers.Builder, uncompressedSize:number) {
  builder.addFieldInt32(7, uncompressedSize, 0);
}

static addCompression(builder:flatbuffers.Builder, compressionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, compressionOffset, 0);
}

static endIoStoreCompressedBlockEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIoStoreCompressedBlockEntry(builder:flatbuffers.Builder, blockIndex:number, ownerTocEntryIndex:number, partitionIndex:number, offset:bigint, ucasOffset:bigint, compressedSize:number, diskSize:number, uncompressedSize:number, compressionOffset:flatbuffers.Offset):flatbuffers.Offset {
  IoStoreCompressedBlockEntry.startIoStoreCompressedBlockEntry(builder);
  IoStoreCompressedBlockEntry.addBlockIndex(builder, blockIndex);
  IoStoreCompressedBlockEntry.addOwnerTocEntryIndex(builder, ownerTocEntryIndex);
  IoStoreCompressedBlockEntry.addPartitionIndex(builder, partitionIndex);
  IoStoreCompressedBlockEntry.addOffset(builder, offset);
  IoStoreCompressedBlockEntry.addUcasOffset(builder, ucasOffset);
  IoStoreCompressedBlockEntry.addCompressedSize(builder, compressedSize);
  IoStoreCompressedBlockEntry.addDiskSize(builder, diskSize);
  IoStoreCompressedBlockEntry.addUncompressedSize(builder, uncompressedSize);
  IoStoreCompressedBlockEntry.addCompression(builder, compressionOffset);
  return IoStoreCompressedBlockEntry.endIoStoreCompressedBlockEntry(builder);
}
}
