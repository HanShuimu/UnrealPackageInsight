"use strict";
// automatically generated by the FlatBuffers compiler, do not modify
Object.defineProperty(exports, "__esModule", { value: true });
exports.IoStorePackageEntry = void 0;
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
const flatbuffers = require("flatbuffers");
class IoStorePackageEntry {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsIoStorePackageEntry(bb, obj) {
        return (obj || new IoStorePackageEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsIoStorePackageEntry(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new IoStorePackageEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    packagePath(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    packageId() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    firstChunkIndex() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    chunkCount() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    firstPartitionIndex() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    firstOffset() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    size() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    compressedSize() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    diskSize() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    order() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    hasPath() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startIoStorePackageEntry(builder) {
        builder.startObject(11);
    }
    static addPackagePath(builder, packagePathOffset) {
        builder.addFieldOffset(0, packagePathOffset, 0);
    }
    static addPackageId(builder, packageId) {
        builder.addFieldInt64(1, packageId, BigInt('0'));
    }
    static addFirstChunkIndex(builder, firstChunkIndex) {
        builder.addFieldInt32(2, firstChunkIndex, 0);
    }
    static addChunkCount(builder, chunkCount) {
        builder.addFieldInt32(3, chunkCount, 0);
    }
    static addFirstPartitionIndex(builder, firstPartitionIndex) {
        builder.addFieldInt32(4, firstPartitionIndex, 0);
    }
    static addFirstOffset(builder, firstOffset) {
        builder.addFieldInt64(5, firstOffset, BigInt('0'));
    }
    static addSize(builder, size) {
        builder.addFieldInt64(6, size, BigInt('0'));
    }
    static addCompressedSize(builder, compressedSize) {
        builder.addFieldInt64(7, compressedSize, BigInt('0'));
    }
    static addDiskSize(builder, diskSize) {
        builder.addFieldInt64(8, diskSize, BigInt('0'));
    }
    static addOrder(builder, order) {
        builder.addFieldInt32(9, order, 0);
    }
    static addHasPath(builder, hasPath) {
        builder.addFieldInt8(10, +hasPath, +false);
    }
    static endIoStorePackageEntry(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createIoStorePackageEntry(builder, packagePathOffset, packageId, firstChunkIndex, chunkCount, firstPartitionIndex, firstOffset, size, compressedSize, diskSize, order, hasPath) {
        IoStorePackageEntry.startIoStorePackageEntry(builder);
        IoStorePackageEntry.addPackagePath(builder, packagePathOffset);
        IoStorePackageEntry.addPackageId(builder, packageId);
        IoStorePackageEntry.addFirstChunkIndex(builder, firstChunkIndex);
        IoStorePackageEntry.addChunkCount(builder, chunkCount);
        IoStorePackageEntry.addFirstPartitionIndex(builder, firstPartitionIndex);
        IoStorePackageEntry.addFirstOffset(builder, firstOffset);
        IoStorePackageEntry.addSize(builder, size);
        IoStorePackageEntry.addCompressedSize(builder, compressedSize);
        IoStorePackageEntry.addDiskSize(builder, diskSize);
        IoStorePackageEntry.addOrder(builder, order);
        IoStorePackageEntry.addHasPath(builder, hasPath);
        return IoStorePackageEntry.endIoStorePackageEntry(builder);
    }
}
exports.IoStorePackageEntry = IoStorePackageEntry;
